name: smartopia_hms_shared
description: Shared library for Smartopia HMS applications.
version: 1.0.10
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0-140.0.dev

# Add regular dependencies here.
dependencies:
  drift: ^2.26.1
  sqlite3: ^2.7.5
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.26.1
  lints: ^5.0.0
  test: ^1.24.0
